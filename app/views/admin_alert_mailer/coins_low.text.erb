AdminAlert#coins_low

Greetings,

The API has generated this email because we are running low on coins.

Current Low Inventory:

Coin Name: <%= @coin[:type] %> 
Number in Inventory: <%= @coin[:number] %>

The total value of all coins in the system is <%= @value %>
